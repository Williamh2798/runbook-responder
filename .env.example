# Optional: enables LLM-powered root-cause analysis instead of rule-based fallback
OPENAI_API_KEY=

# Server
HOST=0.0.0.0
PORT=8000

# Optional: forward new alerts to UiPath webhook (set after configuring API Workflow)
UIPATH_WEBHOOK_URL=
